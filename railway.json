{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "export FLASK_APP=run.py && python -m flask db upgrade || (python -m flask db init && python -m flask db migrate && python -m flask db upgrade) && python run.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}